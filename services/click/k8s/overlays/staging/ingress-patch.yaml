apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: click-ingress
  annotations:
    traefik.ingress.kubernetes.io/router.tls.certresolver: letsencrypt
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
spec:
  ingressClassName: traefik
  tls:
  - hosts:
    - staging-click.wetfish.net
  rules:
  - host: staging-click.wetfish.net
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: click-web
            port:
              number: 80
