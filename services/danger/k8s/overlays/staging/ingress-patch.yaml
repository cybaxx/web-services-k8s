apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: danger-ingress
  annotations:
    traefik.ingress.kubernetes.io/router.tls.certresolver: letsencrypt
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
spec:
  ingressClassName: traefik
  tls:
  - hosts:
    - staging-danger.wetfish.net
  rules:
  - host: staging-danger.wetfish.net
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: danger-web
            port:
              number: 80
